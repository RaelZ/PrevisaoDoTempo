export default interface CityInfo {
  city: string;
  condition_code: string;
  currently: string;
  date: string;
  description: string;
  condition_slug: string;
  wind_speedy: string;
  time: string;
  temp: number;
  humidity: number;
  latitude: number;
  longitude: number;
}
